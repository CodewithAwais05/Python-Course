#  union and intersection

set1 = {1, 2, 5, 4, 7}
set2 = {4, 2, 8, 9, 5}

# union of two sets (combine all elements that are not repeated)
print(set1.union(set2))

# intersection of two sets (common elements of two sets)
print(set1.intersection(set2))