# Module (like a code library) is a file written by another programmer that generally has functions that we can use
# using the os module (os = operating system)

import os
os.remove("fifth.txt")  #remove() function is used to permanently delete the file